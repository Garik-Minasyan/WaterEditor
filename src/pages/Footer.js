import React from 'react';

const Footer = () => {
    return (
        <div className="footer">
            <button>Cancel</button>
            <button>Done</button>
        </div>
    );
};

export default Footer;