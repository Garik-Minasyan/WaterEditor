import React from 'react';

const SetPadding = () => {
    return (
        <div className="padding">
            <p>Padding</p>
            <input type="range" className="paddingInput" />
        </div>
    );
};

export default SetPadding;